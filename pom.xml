<project>
  <!-- Other project configuration -->

  <build>
    <plugins>
      <plugin>
        <groupId>org.php-maven</groupId>
        <artifactId>maven-php-plugin</artifactId>
        <version>2.0.1</version>
        <!-- Configure the plugin as needed -->
      </plugin>
    </plugins>
  </build>
</project>
